extends layout.pug

block content

    div
        a(href='/parks/new') Add New Dog Park
    div
        ul
            each park in parks
                li
                    a(href=`/parks/${park.id}`)= park.name
