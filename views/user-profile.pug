doctype html
html
  head
    title #{title}
    link(rel='stylesheet' href='/stylesheets/reset.css')
    link(rel="stylesheet" href="/stylesheets/profile.css")
    script(src="/javascripts/random-image.js"  type="module")
    script(src="/javascripts/deleteParkFromList.js"  type="module")
  body(id='img')
    header
        div(class="splash")
            a(href="/") good
            span: a(href="/") bois
            div(class='navbar')
                form(action='/users/logout' method='post')
                    button(type='submit' class='logout') Log Out
    br
    br
    main
        div(class='welcome')
            span Welcome #{user.fullName}! You have logged in successfully.
        br
        br
        h2(class='user_name' style )= user.fullName
        h3(class='email')= user.email
        br
        br
        div(class='parkslist') Your Parks list
            each park in parks
                div(id=`${park.id}-parklist-container` class='parklist-container')
                    ul
                        li(id='park')
                            a(href=`/parks/${park.id}`)= park.name
                    button(type='button' id=`delete-parklist-${park.id}` class='buttons' class='delete-parklist-button') Delete Park
                    div(class='popup' id=`popup-${park.id}`)
                        div(class='popup-header') Are you sure you want to delete this park?
                        div(class='popup-body')
                            a(href=`/profile/${park.id}` class='popup-btns' id='cancel') Cancel
                            a(href=`/profile/${park.id}` class='popup-btns delete-yes' id=`yes-delete-${park.id}`) Yes
                            //- button(class='popup-btns delete-yes' id=`yes-delete-${park.id}`) Yes
        br
        br
        //- div(class='reviews') Your Reviews
        //-     each review in reviews
        //-         div(id=`${review.id}-reviews-container` class='reviews-container')
        //-             ul
        //-                 li(id='review')
        //-                      a(href=`/parks/${park.id}`)= review.title
        div(class='moreParks')
            span Want to find more parks for your buddy??
            br
            span Visit
                a(href="/parks")  Parks!
    footer
        div(class="github_links")
            span: a(href="https://github.com/ShanFalk" target="_blank") Shannon Falk
            br
            span: a(href="https://github.com/oliviabir" target="_blank") Olivia Bir
            br
            span: a(href="https://github.com/sonajasani" target="_blank") Sona Jasani
            br
            span: a(href="https://github.com/graceechi" target="_blank") Grace Chi
